{
  "name": "pickleradar",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "mkdir -p logs && : > logs/web.log && PORT=$(sh -c 'p=3000; while lsof -iTCP:$p -sTCP:LISTEN >/dev/null 2>&1; do p=$((p+1)); done; echo $p') && npm run dev --workspace=apps/web -- --port $PORT 2>&1 | tee logs/web.log",
    "build:web": "npm run build --workspace=apps/web",
    "scrape": "npm run start --workspace=packages/scrapers",
    "scrape:pickleballbrackets": "npm run start:pickleballbrackets --workspace=packages/scrapers",
    "scrape:cloud": "gh workflow run scrape.yml && echo 'Scraper triggered! Check progress: gh run list --workflow=scrape.yml -L 1'"
  },
  "devDependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
